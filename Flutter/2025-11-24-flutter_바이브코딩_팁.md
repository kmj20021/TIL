## 스플래시 페이지
앱이 시작될때 잠깐 보여주는 페이지    
주로 로고나 간단한 문구를 보여주면서 앱 로딩 작업을 처리하는 역할을 한다.   
백엔드와의 초기 통신, 사용자 로그인 여부 확인, 필요한 데이터 준비 같은 과정을 수행하는 동안 사용자에게 빈 화면 대신 스플래시 화면을 보여주기 위해 사용된다.   

### 구현 방법
- SplashScreen 위젯을 만든다.   
- initState에서 일정 시간 대기하거나 초기 데이터를 로드한다.   
- 완료되면 Navigator.pushReplacement 또는 Get.offNamed()로 다음 페이지로 이동한다.   

```ruby
class SplashPage extends StatefulWidget {
  const SplashPage({super.key});

  @override
  State<SplashPage> createState() => _SplashPageState();
}

class _SplashPageState extends State<SplashPage> {
  @override
  void initState() {
    super.initState();
    Future.delayed(const Duration(seconds: 2), () {
      Navigator.pushReplacementNamed(context, '/home');
    });
  }

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("앱 로딩 중")),
    );
  }
}
```

## 인증 프로세스
일반적으로는 OAuth2 기술을 이용함

# Copilot Instructions

이 파일은 GitHub Copilot을 사용할 때의 정책과 지침을 기록합니다.

## 정책 목록

### 정책 추가 규칙
- `+++{정책내용}` 형식으로 입력하면 정책내용이 자동으로 파일에 반영됩니다
- 새로운 정책을 추가할 때는 기존 정책과 중복되지 않도록 확인합니다
- 같은 주제의 정책은 잘 결합하여 하나로 통합합니다
- 정책 간 논리적 모순이 없도록 유지합니다

### 응답 형식 정책
- 대답할 때 '네. 주인님({이해도}%)'로 시작합니다
- 이해도가 90% 미만인 경우 이해도를 높일 수 있도록 선택 가능한 질문을 제시합니다
- 사용자가 선택한 질문에 따라 추가 설명을 제공합니다




### 교수님 책 비전공자
https://dimohy.github.io/CodeWithoutCode/

### 교수님 책 전공자
https://dimohy.github.io/CodeWithoutCode/expert