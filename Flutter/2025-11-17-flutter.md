## 라우팅이란?
화면 또는 페이지를 이동하는 기능   
이동 시 화면을 바꾸고 필요한 데이터를 전달하면서 화면 이동 이력을 관리함.

SPA(single page application)
한 화면 안에서 Route만 바뀜
여러 Route가 있음

URI : 집을 식별하는 방법 전체(identifier)
URL : 집의 실제 주소(location)
URN : 집의 고유 이름(name)

## 기본 네비게이터(Navvigator.push)
push -> 페이지 위에 새 페이지 올리기
pop  -> 올렸던 페이지 제거

```ruby
Navigator.push(
  context,
  MaterialPageRoute(builder: (context) => const ProfileScreen()),
);
//
Navigator.pop(context);
```

## Named Routes
main.dart를 다음과 같이 설정하는 방법

```ruby
MaterialApp(
  initialRoute: '/',
  routes: {
    '/': (context) => const HomeScreen(),
    '/login': (context) => const LoginScreen(),
  },
);
//
Navigator.pushNamed(context, '/login');
```

## GetX 라우팅
개발자가 편한 방법
url, 파라미터, 애니메이션 모두 지원

```ruby
GetMaterialApp(
  initialRoute: '/',
  getPages: [
    GetPage(name: '/', page: () => const HomeScreen()),
    GetPage(name: '/login', page: () => const LoginScreen()),
  ],
);
// 이동
Get.toNamed('/login');

// 뒤로가기
Get.back();

// 파라미터 전달
Get.toNamed('/detail?id=3');

// 받기
final id = Get.parameters['id'];
```